# 第三问交付核验记录

日期：2026-09-12。计算、表格、结果分析与三张核心图已交付。

实际输入：题面 `A题/A题.md`，附件环境数据，第二问 `result2.xlsx`，第二问 LaTeX 章节，以及 `A题第三问_AI交接包.md`。原始输入及第一、二问文件未修改。

## 运行与独立核验

- P1：独立最小求解核验通过。`/opt/anaconda3/bin/python solve_q3.py --mode smoke --end 60 --label reviewer_smoke` 退出码 0；另核对独立矩阵计算、边界与全域极值判定。证据见 `../runs/reviewer_invariants.json`。
- 主方案：`/opt/anaconda3/bin/python solve_q3.py --mode case --n 3200 --dt 0.5 --extension mean --label mean_n3200_dt0p5` 实际计算完成，退出码 0。
- 导出：`/opt/anaconda3/bin/python deliver_q3.py` 退出码 0。
- P2：独立复核在同级 `第三问_P2复验_0912` 目录执行 `/opt/anaconda3/bin/python reproduce_q3.py`，退出码 0。此次为哈希匹配缓存重建，不是再次完整运行偏微分方程。独立回执 PASS，无实质问题；证据见 `../../第三问_P2复验_0912/P2终检证据.json`。

P2 逐项核对 result3 的 72429 个含水率数值（3449 个时间点 × 21 个半径）、表5的50个值，均与原始计算数据四位小数结果一致。复现清单8项哈希匹配；重建表格、摘要、正文一致。独立查看一张彩色图和两张灰度图，文字、线型与范围可读。

## 数值与适用条件

主方案采用4 h后末1 h环境均值延拓，报告时长57.4777 h，全域最大含水率为0.14999996901343773 kg/kg，严格小于0.15。末值延拓得到57.1731 h。最近两级空间网格的终点差31.5977 s，最终网格两种时间步的终点差1.4609 s；四位小数为输出格式，局部事件定位的毫秒区间不代表总体预测精度。

当前无交付阻塞。长期边界延拓和既定模型假设仍是结果适用的前提，详见 `../第三问结果与分析.md`。
